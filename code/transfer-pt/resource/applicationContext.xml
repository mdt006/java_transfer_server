<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd  
                http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd  
                http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd">

	<import resource="applicationContext-consumer.xml"/>
	<import resource="applicationContext-provider.xml" />

	<!-- 使用外部的配置文件 -->
	<context:property-placeholder location="file:/etc/dtmnpt_conf.transfer.properties,resource/pt.properties" />
	<bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource">
		<property name="url" value="jdbc:mysql://${mysql_ip}:${mysql_port}/transfer?useUnicode=true&amp;characterEncoding=utf-8&amp;zeroDateTimeBehavior=convertToNull"></property>
		<property name="driverClassName" value="com.mysql.jdbc.Driver"></property>
		<property name="username" value="${mysql_user}"></property>
		<property name="password" value="${mysql_passwd}"></property>
		<!-- 用来检测连接是否有效的sql，要求是一个查询语句 -->
		<property name="validationQuery" value="select 1" />
		<!-- 申请连接的时候检测 -->
		<property name="testWhileIdle" value="true" />
		<!-- 最大并发连接数 -->
		<property name="maxActive" value="100" />
		<property name="filters" value="config,stat" />
		<property name="connectionProperties" value="${mysql_passwd_decrypt}" />
	</bean>
	
	<!-- 会话工厂数据源注入 -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
	</bean>
	
	<!-- redis config start-->
	 <bean id="connectionFactory" class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory">  
        <property name="usePool" value="true"></property>
        <property name="hostName" value="${redis_ip}" />
        <property name="port" value="${redis_port}" />
        <property name="password" value="${redis_passwd}"/>
        <property name="timeout" value="${redis_timeout}" />
    </bean> 
     <bean id="redisTemplate" class="org.springframework.data.redis.core.StringRedisTemplate">    
        <property name="connectionFactory"   ref="connectionFactory" />    
    </bean>  
 	<!-- redis config end-->
 	

	<!-- 扫描 UserMapper.java -->
	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="sqlSessionFactoryBeanName" value="sqlSessionFactory" />
		<property name="basePackage" value="com.ds.transfer" />
	</bean>

	<bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>
	
	<bean id="ptConstants" class="com.ds.transfer.pt.constants.PtConstants" >
      <property name="PT" value="${pt}" />
      <property name="PT_URL" value="${pt_url}" />
      <property name="ENTITY_KEY" value="${entity_key}" />
      <property name="CRT_PWD" value="${crt_pwd}" />
    </bean>
    

	<context:component-scan base-package="com.ds.transfer" />

	<tx:annotation-driven proxy-target-class="true" />

</beans>