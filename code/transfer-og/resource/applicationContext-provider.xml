<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
       http://code.alibabatech.com/schema/dubbo
       http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
  
	<dubbo:application organization="transfer" name="transfer" owner="jackson" />

	<!-- 使用zookeeper注册中心暴露服务地址 -->
	<dubbo:registry address="zookeeper://10.10.230.39:2181?backup=10.10.230.39:2182,10.10.230.39:2183" 
		file="${user.home}/dubbo-service-file/transfer/dubbo.cache" />

	<dubbo:protocol name="dubbo" port="20885" host="${host}"/>

	<!-- 声明需要暴露服务的接口 -->
	<dubbo:service interface="com.ds.transfer.common.service.TransferService" ref="transferService" 
		group="og" version="1.0.0" timeout="60000" retries="0" owner="jackson" />

	<bean id="transferService" class="com.ds.transfer.og.service.TransferServiceImpl" />

</beans>